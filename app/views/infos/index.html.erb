<h1>Listing infos</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Date uploaded</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @infos.each do |info| %>
  <tr>
    <td><%= info.title %></td>
    <td><%= info.description %></td>
    <td><%= info.date_uploaded %></td>
    <td><%= link_to 'Show', info %></td>
    <td><%= link_to 'Edit', edit_info_path(info) %></td>
    <td><%= link_to 'Destroy', info, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Info', new_info_path %>
